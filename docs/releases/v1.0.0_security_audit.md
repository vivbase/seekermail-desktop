# v1.0.0 GA Security & Privacy Audit (T117)

**Scope:** code repo HEAD + macOS installer + Windows installer. Builds on the T103
no-proxy compliance work and extends it to the GA bar. Automated items are verified
by `scripts/security_audit.sh`; manual items (packet capture) are captured by the
Release Engineer.

**Legend:** ✅ PASS · ⚠️ WARNING (mitigation noted) · ❌ FAIL (blocks T118).

## How to reproduce

```bash
scripts/security_audit.sh /path/to/seekermail.log   # log path optional
cargo test --manifest-path src-tauri/Cargo.toml --test compliance
```

## Audit items

| #   | Item                                                  | Method                                                                                                                                                                     | Status                                                           |
| --- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| 1   | **No telemetry**                                      | `grep -RniE 'sentry\|datadog\|amplitude\|mixpanel\|segment\|bugsnag' src-tauri/Cargo.toml` → no match                                                                      | ✅ PASS                                                          |
| 2   | **Analytics off by default**                          | `app_settings."privacy.analytics_enabled"` seeded `false` (migration 001)                                                                                                  | ✅ PASS                                                          |
| 3   | **Data sovereignty**                                  | `body_text` / `body_html` are read in `ai/` only inside context assembly, which posts to the user-configured `account_ai_settings.ai_base_url` (never a SeekerMail server) | ✅ PASS (static check in `security_audit.sh`)                    |
| 4   | **No-proxy egress**                                   | `cargo test --test compliance` green (no-proxy + log-safety) on macOS and the Windows matrix                                                                               | ✅ PASS (CI)                                                     |
| 5   | **Secrets never in SQLite**                           | All credentials go through the `Keychain` trait (macOS Keychain / Windows Credential Manager); `git grep` finds no secret literals in `keychain/`                          | ✅ PASS                                                          |
| 6   | **Log safety**                                        | `security_audit.sh` greps the real `seekermail.log`: no mail body, no address, no secret/token literals                                                                    | ⚠️ run against the real log before sign-off                      |
| 7   | **Update check is the only outbound SeekerMail call** | `tauri.conf.json` updater endpoint sends only `{target}/{arch}/{current_version}` (no content)                                                                             | ✅ PASS                                                          |
| 8   | **Windows packet capture (1h)**                       | Fiddler/Wireshark allowlist: user mail servers + BYO AI endpoint + `updates.seekermail.app` only                                                                           | `[CAPTURE: windows_noproxy_1h.png]` ⚠️ pending                   |
| 9   | **Third-party audit readiness**                       | This document + signed installers are the hand-off package; no open Critical items                                                                                         | ✅ PASS (engineering side; external audit tracked by PM in T118) |

## Residual risk / notes

- The Windows Credential Manager round-trip (T114) and the 1-hour Windows packet
  capture (#8) require a real Windows user session; they are confirmed on a
  self-hosted/physical Windows runner and the screenshot committed before T118.
- No `❌ FAIL` items. Item #6 and #8 are `⚠️` only because they depend on a live
  run the Release Engineer performs on real hardware at tag time.

_Automated rows generated by `scripts/security_audit.sh`. Replace the `[CAPTURE: …]`
placeholder with the committed evidence before T118 sign-off._
