{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for SeekerMail windows — the main window AND detached workbench-* windows. Grants the core webview APIs, notifications, and window start-dragging (needed by the custom parchment titlebar's data-tauri-drag-region). Custom #[tauri::command] handlers are allowed by default.",
  "windows": ["main", "workbench-*"],
  "permissions": ["core:default", "notification:default", "core:window:allow-start-dragging"]
}
