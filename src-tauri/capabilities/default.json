{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities granted to the main SeekerMail window. Custom #[tauri::command] handlers are allowed by default; this grants the core webview APIs only.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
